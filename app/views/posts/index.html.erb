<h1>Welcome to the Cove!</h1>
<p id="alert"><%= alert %></p>
<%= link_to "Post your Dream", new_post_path %>
<div>
    <table class="table">
         <thead>All Dreams</th>

        <tr>
            <th>Title</th>
            <th></th>
            <th style="text-align: right">Posted</th>

        </tr>

        <% @posts.each do |post| %>
            <tr>
                <td><%= link_to post.title, post_path(post) %><td>
                <td style="text-align: right"><%= post.created_at.to_date %><td>
            </tr>
        <% end %>
    </table>
</div>